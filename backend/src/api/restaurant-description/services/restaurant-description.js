'use strict';

/**
 * restaurant-description service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::restaurant-description.restaurant-description');
